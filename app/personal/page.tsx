export const dynamic = 'force-dynamic';

import HomeClient from './HomeClient';

export const metadata = { title: 'Ishin Personal — Japanese ⇄ English translator' };

export default function Page() {
  return <HomeClient />;
}
